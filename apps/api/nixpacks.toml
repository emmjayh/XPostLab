[phases.install]
cmds = ['cd ../.. && pnpm install --frozen-lockfile']

[phases.build] 
cmds = ['cd ../.. && pnpm build --filter=@ai-content-coach/api']

[start]
cmd = 'cd ../../apps/api && pnpm start'