[phases.install]
cmds = ['cd ../.. && pnpm install --frozen-lockfile']

[phases.build] 
cmds = ['cd ../.. && pnpm build --filter=@ai-content-coach/web']

[start]
cmd = 'cd ../../apps/web && pnpm start'