[phases.setup]
nixPkgs = ['nodejs_18', 'pnpm']

[phases.install]
cmds = [
    'cd ../..',
    'pnpm install --frozen-lockfile'
]

[phases.build]
cmds = [
    'cd ../..',
    'pnpm run build --filter=@ai-content-coach/web'
]

[start]
cmd = 'cd ../../apps/web && pnpm start'